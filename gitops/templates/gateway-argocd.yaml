# Copyright (c) 2024-2026 Accenture, All Rights Reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#         http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: argocd-route
  namespace: {{ .Values.config.namespacePrefix }}argocd
  labels:
    gateway: gke-gateway
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  parentRefs:
    - kind: Gateway
      name: gke-gateway
      namespace: {{ .Values.config.namespacePrefix }}gke-gateway
      sectionName: http
  hostnames:
    - {{ .Values.config.domain }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /argocd
      filters:
        - type: URLRewrite
          urlRewrite:
            hostname: {{ .Values.config.domain }}
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /argocd
      backendRefs:
        - name: {{ .Values.config.namespacePrefix }}argocd-server
          port: 80
---
apiVersion: networking.gke.io/v1
kind: HealthCheckPolicy
metadata:
  name: argocd-healthcheck
  namespace: {{ .Values.config.namespacePrefix }}argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  default:
    checkIntervalSec: 15
    timeoutSec: 15
    healthyThreshold: 1
    unhealthyThreshold: 2
    logConfig:
      enabled: true
    config:
      type: HTTP
      httpHealthCheck:
        port: 8080
        requestPath: /healthz?full=true
  targetRef:
    group: ""
    kind: Service
    name: {{ .Values.config.namespacePrefix }}argocd-server
---
# Argo CD UI keeps long-lived streams (WebSocket / gRPC-Web) to the API server for live Application sync.
# Default Gateway/LB backend timeouts can close those connections; the SPA then reconnects and can appear to
# refresh the whole UI. Align with gateway-argo-workflows / mtk-connect long-connection backends.
apiVersion: networking.gke.io/v1
kind: GCPBackendPolicy
metadata:
  name: argocd-server-backendpolicy
  namespace: {{ .Values.config.namespacePrefix }}argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  default:
    timeoutSec: 86400
  targetRef:
    group: ""
    kind: Service
    name: {{ .Values.config.namespacePrefix }}argocd-server
---
# Allow ArgoCD Server ingress from Gateway
# Security is enforced by GCP Load Balancer and HTTPRoute configuration
{{- if .Values.config.enableNetworkPolicies }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-argocd-server-ingress-from-gateway
  namespace: {{ .Values.config.namespacePrefix }}argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: argocd-server
  policyTypes:
  - Ingress
  ingress:
  - ports:
    - protocol: TCP
      port: 80     # HTTP traffic
    - protocol: TCP
      port: 443    # HTTPS traffic
    - protocol: TCP
      port: 8080   # Health check port

{{- end }}
